/*
 * ZoneSummary Messages
 *
 * This contains all the text for the ZoneSummary container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ZoneSummary';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ZoneSummary container!',
  },
});
